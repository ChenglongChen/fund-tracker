node_modules
dist
build/
build
.DS_Store
.env
.env.*
!.env.example

# Screenshot / local API 临时数据
.tmp/screenshot-data/

# Python / ML 遗留（项目已无 ML 模块）
.venv/
.pytest_cache/

# Mac 图标构建产物（npm run mac:build 再生）
apps/mac/build/

# 运行时 / 个人持仓（首次启动从 src/portfolio.json 复制）
data/portfolio.json
data/app-state.json
data/day-display-state.json
data/impact-snapshots.json
data/valuation-profiles.json

# 私有验收基准（复制 *.example.json 后填写）
*.local.json
scripts/fixtures/*.local.json

# 本地临时文件（校准日志、东财抓包、PDF、备份等）
data/_*
data/.backup/
data/_calibrate-run.log
data/*.pdf
data/*.js

# 临时脚本 / scratch（保留 .tmp/README.md）
.tmp/*
!.tmp/README.md
