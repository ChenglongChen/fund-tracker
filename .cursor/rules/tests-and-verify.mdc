---
description: 测试与验收脚本约定
globs: server/**/*.test.js,scripts/verify-*.js,scripts/**/*.js
alwaysApply: false
---

# Tests & Verify

## 单元测试

```bash
npm run test:fund-estimate              # server/fund-estimate.test.js
npm run test:realtime-profit            # server/realtime-profit-consistency.test.js
npm run test:display-session              # phase/snapKey 唯一接口
npm run test:live-pipeline              # ground truth 流水线
npm run test:display-state              # snap 状态机
npm run test:realtime-display-pipeline  # suppress + snap + header=Σrow1
node server/market-session.test.js
```

改 `fund-estimate.js`、`market-session.js`、`components/snap-*.js`、`live.js`、`aggregate.js`、`holdings-pipeline.js`、`market.js`（含 `refreshFundHoldingsDisplay`）后至少跑 fund-estimate + realtime-profit + live-rt1-holdings；动 snap/suppress 加跑 display-state + realtime-display-pipeline。

## 集成验收（API :8788）

```bash
npm run verify:alipay-realtime   # 支付宝 Σ estimateProfit = row1
npm run verify:tab-reconcile     # 三 tab RT1 互证；baseline+RT1=EST
```

## 断言习惯

- 盘前/盘后：RT1 用 regular only，不含 extended
- A 股 suppress 窗口（21:30–09:30 + 周末）：`impactPct` / `estimateProfit` 为 null；**snap 后仍为 null**
- Snap 阶段：`header RT1 === Σ estimateProfit`；第二次 apply 不改变 totals（除非 reseed）
- provisional snap（空 funds）不得冻结列表
- QDII US 正盘：`/api/fund/022184/detail` 中华泰/腾讯等 HK closed 持仓 `changePct==null` 且 `liveRt1Excluded==true`
- EST：`fundEstimatedAssets` 为 `amount+ep`；禁止 `amount−settled+ep`
