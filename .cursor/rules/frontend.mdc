# Frontend

主入口：`src/main.js`（页面编排）。**业务数据与组件分离**。

## 分层

| 层 | 路径 | 职责 |
|----|------|------|
| ViewModel | `live-view-model.js`, `summary.js` | API → 行 / scope 合计 |
| Components | `components/metrics.js`, `components/session.js` | 可复用指标 UI |
| Format | `format.js`, `display-format.js` | 数字/金额/隐私 |

详见 `docs/frontend-architecture.md`。

## buildSummary

```javascript
// SCOPE_ALL：canonicalTotals = live.totals，displayState = live.displayState
// 账户 scope：Σ estimateProfit；禁止 pct×amount 作 row1
```

## mergeLiveIntoFunds

```javascript
// live-view-model.js — row1 = API estimateProfit only
state.fundRows = mergeLiveIntoFunds(FUNDS, live);
```

## 组件（模式 A / B）

- **模式 A**：`renderPctSub` + `fmtMoney` — 无 extended
- **模式 B**：`renderRealtimeSplitRow` / `renderMetricDualLine` — 盘前/盘后双行

Hero、账户卡、列表 **必须** 共用 `components/metrics.js`，禁止复制 HTML 结构。

## patch 优先

列表 1s 刷新：`patchListDom()` → 失败再 `paint()`。
