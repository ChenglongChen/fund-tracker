---
description: fund-tracker 项目总则与文档入口
alwaysApply: true
---

# fund-tracker

Node 18+ ESM 项目：`server/` API + `src/` Vite SPA（iPhone PWA）。

## 必读

| 优先级 | 文档 |
|--------|------|
| 1 | `AGENTS.md` |
| 2 | `docs/realtime-spec.md` |
| 3 | `docs/data-flow.md` |
| 4 | `docs/architecture.md` |
| 5 | `docs/frontend-architecture.md`（改 UI） |

## 后端硬性约束

- ep 只在 `fund-display.js` 计算；session 只在 `display-session.js` 解析
- 编排只用 `live-pipeline.runLiveDisplayPipeline()`
- Header EST：**live** = `Σ estimateAssets`；**snap**（day_open/eod_freeze）= `B[D]+RT1`（见 `applyPortfolioTotalsSnap`）
- QDII T+1：`liveRt1Only` + `maskHoldingsForLiveRt1Display`（`computeFundImpactFromPack` **与** `refreshFundHoldingsDisplay` 两处必调）
- A 股 suppress 三处 + `finalizeLiveFundDisplayRow`
- settle 不得 clear snap

## 前端硬性约束

- row1 只读 API `estimateProfit`（`live-view-model.js`）
- SCOPE_ALL Hero 用 `live.totals`；账户 Tab 用 `totalsByAccount`；**禁止** `amount−settled+ep`
- 详情穿透：`liveRt1Excluded` → 涨跌幅/状态均 **`—`**（`components/session.js`）
- 禁止在 main.js 新增内联指标 HTML — 抽组件

## 改动后

**大改（RT1/EST/snap/suppress/穿透/aggregate/display-session）合并前必跑：**

```bash
npm run test:regression
npm run build   # 动 src/
```

`test:regression` = 28 组单元测试串联（见 `package.json`）。小改可只跑相关子集，但交付前仍建议全量 regression。

## 临时文件

- 一次性调试脚本、抓包、本地试验 → **`.tmp/`**（已 gitignore；说明见 `.tmp/README.md`）
- 长期保留的校准/验收脚本 → `scripts/`
- 勿把临时文件散放在仓库根目录或 `data/`
